@{
    ViewBag.Title = "Manage Gateways";
}
 
<div class="row">
    <div class="col-md-12">
        <div class="card-header">
            <i class="fa fa-table"></i> Gateways List  
            <span class="pull-right">
                @Html.ActionLink("Create New", "Create", "Gateways", null, new { @class = "btn btn-primary btn-sm" })
            </span>
        </div>
        <div class="card-body">
            @Html.Partial("_DataTable")
        </div>
        <div class="card-footer small text-muted">Updated yesterday at 11:59 PM</div>
    </div>
</div>