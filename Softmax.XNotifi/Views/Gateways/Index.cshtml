@{
    ViewBag.Title = "Gateways List";
}
 
<div class="row">
    <div class="col-md-12">
        <div class="card-header">
            <i class="fa fa-table"></i> @ViewBag.Title
            <span class="pull-right">
                @Html.ActionLink("Create New", "Create", "Gateways", null, new { @class = "btn btn-primary btn-sm" })
            </span>
        </div>
        <div class="card-body">
            @Html.Partial("_DataTable")
        </div>
        <div class="card-footer small text-muted">Last Update @DateTime.Now</div>
    </div>
</div>